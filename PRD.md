# PRD：呼吸康复吹气训练小程序（个人测试版）

**版本**: v1.0（草案）
**状态**: DRAFT（待用户确认）
**编制**: 项目组 | 日期：2026-08-14
**目标**: 2-3 天出第一个可用版本（微信小程序体验版，不发布）

---

## 0. 变更记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-08-14 | 初始 PRD：吹气检测 + 蜡烛可视化 + 4 训练模式 + 历史趋势 |
| v1.0 | 2026-08-14 | 新增：用户档案 + 首次基线测试 + 循证康复指导对照（基于 Post-ICU 康复方案） |

---

## 1. 背景与定位

### 1.1 背景

用户（ICU 转出进入康复期）。医生确认：血氧下降源于痰阻塞气道而非神经性呼吸驱动不足——**自主呼吸驱动 OK，问题是"排痰"和"呼吸肌力量"**（依据：飞书《Post-ICU-Rehabilitation-Comprehensive-Plan》）。

家庭康复需要一种**低成本、可每日进行、有即时反馈**的呼吸训练方式：对手机吹气，小程序实时测分贝与时长，游戏化蜡烛动画反馈，长期记录趋势。

### 1.2 产品定位

- **非医疗器械的家庭辅助训练工具**：不诊断、不治疗、不替代肺功能检查。
- **个人自用**：仅给用户（及家属辅导）使用，微信小程序体验版，不发布、不上架。
- **数据本地化**：所有数据存于手机本地，无后端、无云端同步。

### 1.3 目标用户

| 角色 | 描述 | 使用方式 |
|------|------|---------|
| 用户 | 康复期患者，肺功能偏弱 | 每日按引导完成训练 |
| 家属 | 设置档案、辅导使用、查看趋势 | 辅助配置与查看 |

### 1.4 成功指标（个人测试版）

- 首次使用 10 分钟内完成档案填写 + 基线测试
- 每日完成 3-5 次训练（循证依从性目标）
- 训练后即时反馈"比上次/比基线"的进步与否
- 历史页能看出 1 周以上的趋势变化

---

## 2. 核心功能

### 2.1 用户档案（首次进入必填）

首次打开小程序，进入**档案填写页**，必填项（档案将随每个康复周期 Session 快照存档）：

| 字段 | 类型 | 选项/说明 |
|------|------|----------|
| 称呼/姓名 | 文本 | 如"王阿姨"（仅本地显示，可自拟） |
| 身份 | 单选 | 健康人 / 病人 |
| 年龄 | 数字 | 出生年或周岁 |
| 性别 | 单选 | 男 / 女 |
| 原发病（基础疾病） | 多选+文本 | 如：脑梗、肺炎、心功能不全、COPD、其他（自填） |
| 近期是否做过手术 | 单选 | 是 / 否 + 手术类型 + 时间 |
| 是否 ICU 经历 | 单选 | 是 / 否（若是：ICU 时长、是否已拔管/脱机） |
| 康复阶段 | 单选 | 围手术期 / ICU 后康复 / 慢性康复 / 健康日常 |
| 使用设备 | 单选 | 手机（距离说明：约 15-20cm） |

> 档案用于：① 生成**相对自身的参考值区间**（基于年龄/性别/疾病/手术/ICU 等条件）；② 训练目标建议；③ 记录页展示。

### 2.2 康复周期 Session + 基线测试（核心 · 新增）

**康复周期（Session）**是本小程序的数据组织核心：

- 新用户首次进入 → 自动创建**第 1 个康复周期 Session**，档案快照入 Session。
- **可多次新建 Session**（如：手术后开始新周期、ICU 出院后新周期、阶段复查后新周期）。
- **每个新 Session 都必须重新录制基线**（旧 Session 的基线不跨周期复用——康复进程中基线会提升，跨周期对比无意义）。
- 每次训练记录**必须挂载到某个 Session**，并记录"与当前 Session 基线"的相对差值。

**基线测试**（每个 Session 建立时执行）：引导用户完成**三口气 × 3 次重复**的采集：

| 测试组 | 力度引导 | 目标 | 记录指标 |
|--------|---------|------|---------|
| 第 1 口气 | 轻轻吹（像吹蜡烛但不灭） | 低强度基线 | 峰值分贝、持续时长 |
| 第 2 口气 | 普通吹（正常力度呼气） | 中强度基线 | 峰值分贝、持续时长 |
| 第 3 口气 | 使劲吹（全力呼气） | 最大强度基线 | 峰值分贝、持续时长 |

**每组重复 3 次**（共 9 次吹气），每次间隔休息 10 秒，取中位数作为该组基线值。

**基线跳过规则（新增）**：
- 基线测试**允许跳过**（极限测试不强制）。
- 但若档案表明处于**围手术期 / ICU 后康复**阶段，页面明确建议"先完成基线再训练"，跳过后训练页常驻"未测基线，建议先测"提示 + 随时可补测入口。
- 跳过基线时：Session 可正常训练，训练记录仍保存，`vsBaseline` 标记为 `null`（不比较），待基线补录后自动追溯补算。

**基线输出（记录为 `baseline` 快照）**：

| 指标 | 含义 | 判定 |
|------|------|------|
| 轻吹峰值分贝 | 基础呼吸肌力 | 综合三组与"相对自身参考值"给出：强 / 中等 / 偏弱 |
| 普通吹峰值分贝 | 日常呼气能力 | 同上 |
| 劲吹峰值分贝 | 最大呼气力量（PEF 趋势代理） | 同上 |
| 每口气持续时长 | 呼气耐力 | 时长越短越偏弱 |
| 综合评级 | 加权评分 | 强 / 中等 / 偏弱（相对自身判定，不宣称医学诊断） |

**相对自身参考值（依据档案生成）**：
- 输入：年龄、性别、身份（健康人/病人）、原发病、手术/ICU 经历、康复阶段。
- 输出：针对"轻/普通/劲"三档的**参考分贝区间 + 参考持续时长**（如：63 岁女性、ICU 后康复 → 劲吹参考区间 65-85dB、持续 3-6s）。
- 参考值由 `utils/params.js` 中的**规则表**生成（年龄分层 × 状态调整），仅用于话术参照与判定语境，**不宣称临床参考值**（页面标注"参考区间为经验值，非医学标准"）。
- v1 不做 GLI-2012 / 年龄性别分层表的正式方程（需要标定），留 v2。

**首次判定话术**（示例）："您目前的第一口气（相对参考值）属于**偏弱**水平，建议从**蜡烛模式**开始，每日 3 次。"

> ⚠️ 判定为**相对参照**（相对同龄/性别参考范围与训练强度区间），页面必须标注"非医学诊断，仅供参考"。

### 2.3 吹气检测（核心 · 原 PRD）

- 实时分贝：`onFrameRecorded` 拿 PCM 帧 → 计算 RMS → 换算分贝（0-120dB）
- 吹气时长：分贝 > 阈值（如 50dB）开始计时，低于阈值停止
- 吹气强度（峰值）：记录吹气过程最高分贝
- 稳定性：分贝标准差/平均值，数值越小越稳定
- 波形复杂度（可选，v2）：FFT 频谱分析

### 2.4 蜡烛/火焰可视化

| 分贝范围 | 蜡烛状态 |
|----------|---------|
| < 40 dB | 蜡烛不动（环境噪音） |
| 40-60 dB | 蜡烛微摇（轻吹） |
| 60-80 dB | 蜡烛大摇（中等力度） |
| 80-100 dB | 灭了一根蜡烛（用力吹） |
| > 100 dB | 全部蜡烛灭（最大力度） |

### 2.5 训练模式（4 种）

| 模式 | 玩法 | 测什么 |
|------|------|--------|
| 蜡烛模式 | 吹灭 5 根蜡烛（每根需达阈值） | 峰值力度 |
| 持久模式 | 保持火焰"大摇"状态尽可能久 | 呼吸耐力 |
| 节奏模式 | 按提示"吹-停-吹-停"交替 | 呼吸控制 |
| 自由模式 | 随便吹，记录数据 | 基线/随意 |

### 2.6 数据记录 + 趋势

记录：日期/时间、训练模式、峰值分贝、吹气时长、稳定性评分、与基线差值。

展示：
- 历史训练记录列表
- 折线图（wx-canvas）：峰值分贝、吹气时长随时间变化
- 当日总结："比昨天好/差"

### 2.7 循证康复指导对照（新增）

基于飞书 Post-ICU 康复方案（SCCM 2024 指南 + ICU 后综合征管理共识），在结果页/历史页提供**非医学建议**的引导话术：

- **排痰导向**：鼓励持续呼气训练（与"气道廓清"目标一致），训练提示"深吸气 → 缓慢长呼气"
- **呼吸肌力量**：基线偏弱 → 推荐蜡烛模式（峰值训练）；耐力偏弱 → 推荐持久模式
- **依从性**：每日 3-5 次，记录连续天数，达到后给出鼓励
- **趋势**：相对自身基线持续进步 → "您的呼气能力正在稳步恢复"；连续 3 天退步 → 提示"建议咨询医生"（非诊断，仅提醒）

> 指导话术必须克制：只做"趋势对比 + 鼓励 + 就医提醒"，不做"诊断结论"。

---

## 3. 页面结构

| 页面 | 路径 | 功能 |
|------|------|------|
| 档案 | pages/profile | 首次必填，可后续修改 |
| 基线测试 | pages/baseline | 首次强制，9 次吹气采集 + 判定 |
| 主页 | pages/index | 模式选择 + 今日状态 + 康复周期管理入口 |
| 训练 | pages/train | 录音 + 分贝 + 蜡烛动画 |
| 历史 | pages/history | 记录列表 + 折线图 + 当日总结 |
| 周期 | pages/session | 康复周期列表 + 新建周期 + 周期内基线重录 |

路由规则（启动分流）：
1. 无档案 → 档案页（必填）
2. 有档案、无任何 Session → 自动创建 Session 1 → 基线测试（可跳过）→ 主页
3. 当前 Session 无基线 → 建议补测（可跳过）→ 主页
4. 正常 → 主页（训练/历史/周期随意进出）

新建 Session 流程：周期页点"新建康复周期" → 确认档案快照（可更新原发病/手术/ICU 等）→ 新基线测试 → 成为当前 Session。

---

## 4. 技术实现细节

### 4.1 录音 + 实时分贝（核心代码逻辑）

```javascript
const recorder = wx.getRecorderManager()

recorder.onFrameRecorded((res) => {
  const data = new Int16Array(res.frameBuffer)
  let sum = 0
  for (let i = 0; i < data.length; i++) sum += data[i] * data[i]
  const rms = Math.sqrt(sum / data.length)
  const db = 20 * Math.log10(rms / 32767) + 90  // 粗略换算
  updateCandleAnimation(db)
})

recorder.start({
  duration: 300000,
  sampleRate: 16000,
  numberOfChannels: 1,
  encodeBitRate: 48000,
  format: 'pcm',
  frameSize: 5
})
```

### 4.2 本地数据模型（Session 制）

```javascript
// 用户档案（独立存储，Session 建立时快照）
{
  name: '用户', role: 'patient'|'healthy', age: 63, gender: 'female',
  diseases: ['脑梗','肺炎','心功能不全'], surgery: {done:true, type:'', time:'2026'},
  icu: {admitted:true, duration:'2周', extubated:true},
  rehabPhase: 'postICU'|'perioperative'|'chronic'|'daily',
  createdAt: '2026-08-14T10:00:00'
}

// 康复周期 Session（核心组织单元，可多个）
{
  id: 'sess-001', createdAt: '2026-08-14T10:05:00',
  title: '康复周期 1', note: 'ICU出院后第1周期',
  profileSnapshot: { ...档案快照, takenAt: '...' },   // 该周期建立时的档案
  baseline: { ...基线快照 } | null,                    // 每个周期独立重录
  baselineHistory: [ ...旧基线快照 ],                  // 周期内重测历史
  status: 'active'|'archived'
}

// 基线快照
{
  version: 1, createdAt: '2026-08-14T10:10:00',
  levels: {
    light:  { peakDb: 62, durationSec: 3.2, reps: [61,62,63] },   // 3次重复取中位数
    normal: { peakDb: 71, durationSec: 4.1, reps: [70,71,72] },
    hard:   { peakDb: 82, durationSec: 5.0, reps: [81,82,83] }
  },
  overall: 'weak'|'medium'|'strong'
}

// 训练记录（挂载到 Session）
{
  id: 'uuid', sessionId: 'sess-001',
  ts: '2026-08-14T19:00:00', mode: 'candle'|'endurance'|'rhythm'|'free',
  peakDb: 78, durationSec: 6.2, stability: 0.18,
  vsBaseline: {db: +7, dur: +1.2} | null   // 相对当前Session基线的差值；无基线=null
}
```

存储键：`profile` / `sessions`（数组，含基线）/ `trainings`（数组，引用 sessionId）/ `activeSessionId` / `settings`。

### 4.3 循证医学参数对照（趋势性参考）

| 临床指标 | 小程序对应 | 说明 |
|----------|-----------|------|
| PEF (L/min) | 劲吹峰值分贝 | 相对趋势，不做绝对值诊断（63 岁女性正常 > 250 L/min 仅作背景参考） |
| FEV1 (L) | 稳定 >50dB 的持续秒数 | 粗估，仅趋势 |
| 呼吸肌力量 | 峰值分贝 + 稳定性 | 综合评分 |
| 训练依从性 | 每日训练次数 | 目标 3-5 次/日 |

---

## 5. 开源参考项目

| 项目 | 链接 | 借鉴 |
|------|------|------|
| xiangyuecn/Recorder | https://github.com/xiangyuecn/recorder | JS 录音 + 波形可视化 |
| ConnorGodBike/mini-waveview | https://github.com/ConnorGodBike/mini-waveview | 小程序波形显示 |
| Samuelson777/Simple-Sound-Level-Meter | https://github.com/Samuelson777/Simple-Sound-Level-Meter | RMS 计算逻辑 |
| 微信 RecorderManager 文档 | https://developers.weixin.qq.com/miniprogram/dev/api/media/recorder/RecorderManager.html | API 参考 |

---

## 6. 里程碑与验收

| 里程碑 | 内容 | 验收 |
|--------|------|------|
| M1 环境 | 微信开发者工具 + 测试号项目 | 可编译运行空项目 |
| M2 档案 | profile 页填写/存储/校验 | 建档后可进入基线 |
| M3 周期+基线 | session 页（新建周期/快照）+ baseline 页 9 次吹气 + 判定 + 跳过逻辑 | 完成 3 口气 × 3 次 → 出基线快照；可跳过；新周期重录基线 |
| M4 核心训练 | train 页录音 + 分贝 + 蜡烛 | 实时分贝正确、蜡烛响应 |
| M5 模式 | 4 种训练模式逻辑 | 每种模式可完成并出结果 |
| M6 历史 | history 列表 + 折线图 + 总结 | 训练后记录可见、趋势可见、相对基线差值可见 |
| M7 体验版 | 上传体验版 + 添加微信号 | 真机可运行完整流程 |

## 7. 后期增强（v2）

- FFT 频域分析（更精细评估）
- 呼吸节律引导动画（吸气 3s → 呼气 6s）
- 语音播报（"今天比昨天进步了"）
- 数据导出 CSV（给医生）
- 多用户 + 后端（Rust + 数据库，给江苏团队）
- 峰流速仪标定（dB → L/min 换算公式）
- 正式年龄/性别分层参考表（GLI-2012 等，替代经验规则表）

---

## 8. 决策记录（2026-08-14 用户确认）

| # | 决策 | 结论 |
|---|------|------|
| D1 | 基线判定方式 | 相对自身参考值（档案规则表），不做 GLI 分层（v2 再做） |
| D2 | 基线是否可跳过 | 可跳过；围手术期/ICU 后康复建议完成，训练页常驻补测提示 |
| D3 | 健康人/病人话术 | v1 统一话术（说明小程序用途即可），不做区分 |
| D4 | 真机验证范围 | 先安卓；iOS 视差异情况后补 |
| D5 | 康复周期管理 | Session 制：可多次新建周期，每周期重录基线，训练挂载周期并记录相对基线差值 |
| D6 | 档案字段 | 含原发病、手术、ICU 经历、康复阶段，随 Session 快照存档 |

---

项目组 | 2026-08-14
